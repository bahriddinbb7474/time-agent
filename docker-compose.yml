services:
  bot:
    build: .
    container_name: time_agent_bot
    env_file:
      - .env
    environment:
      - TZ=${TZ:-Asia/Tashkent}
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
    restart: always